
/**
 * This class contains code that
 * can find the largest element in an array.
 */

public class LargestElementInArray{

    /**
     * in the following code
     * we initialize the array,
     * we initialize maxElement as the first element in the array
     * we iterate through the array
     * while iterating we check if each element is greater than maxElement
     * if the one of the elements are greater,
     * we give maxElement the value of that element
     * after iterating through the array
     * we print maxElement,
     * thus giving us the largest element in the array.
     */
    public static void main(String[] args){
        // Initializing the array
        int[] array = {10 , 23 , 9};
        //Initialize max as first element in array
        int maxElement = array[0];

        //iterate through the array
        for (int i = 0 ; i < array.length ; i++) {
            //Comparing the elements with maxElement
            if (array[i] > maxElement) {
                maxElement = array[i];
            }
        }
        System.out.println("The largest element is " + maxElement);
    }
}
